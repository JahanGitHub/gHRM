
@{
    ViewBag.Title = "Category Setup";
    Layout = "~/Views/Shared/_AdminLteLayout.cshtml";
}

<style type="text/css">
    th {
        height: 40px;
        vertical-align: top;
        margin-top: 0px !important;
        padding-top: 0px !important;
    }

    span.jtable-column-header-text {
        margin-top: 0px !important;
    }
</style>


<script type="text/javascript">
    function GetByID(ids, status) {

        var data = GenerateAjaxRequist('/Common/GetByCategoryID?ids=' + ids + '&status=' + (status == 1 ? 'u' : 'd'), null, "POST")
        if (status == 1) {
            $("#hdnCategoryID").val(data.Records.CategorySubCategoryID);
            $("#txtCateName").val(data.Records.CategorySubCategoryName);
            $("#txtCategoryInBangle").val(data.Records.NameInBangla);
            $("#txtCateCode").val(data.Records.CateorSubCateCode);
            $("#txtCateCode").prop("disabled", true)
            $("#chkAutoGenerate").prop("disabled", true)
            $("#btnSave").val("Update");
        }
        else if (status == 2) {
            $.alert.open("Success", data.message);
            reloadGrid();
        }

    }
    $(document).ready(function () {
        //NumericTextboxOnly("#txtCateCode");
        $("#hdnCategoryID").val(0);
        $("#chkAutoGenerate").on('click', function () {
            var bool = false;
            if ($(this).is(':checked'))
                bool = true;
            $("#txtCateCode").prop("disabled", bool);
            if (true) $("#txtCateCode").val('');
        });
        $("#btnSave").on('click', function () {
            debugger;
            var status = "";
            if ($("#hdnCategoryID").val() == "0") status = "I";
            else
                status = "U";
            if ($("#txtCateName").val()) {
                var dataObject = {
                    CategorySubCategoryID: $("#hdnCategoryID").val(),
                    CategorySubCategoryName: $("#txtCateName").val(),
                    NameInBangla: $("#txtCategoryInBangle").val(),
                    CateorSubCateCode: $("#txtCateCode").val(),
                    ParentCategoryID: 0,
                    IsActive: true,
                };
                var data = GenerateAjaxRequist('/Common/CategorySetup?status=' + status + '&isAutoGenerate=' + $("#chkAutoGenerate").is(':checked'), JSON.stringify({ obj: dataObject }), "POST")
                $.alert.open("Success", data.message);
                ClearFields();
                $("#hdnCategoryID").val(0);
                $("#btnSave").val("Save")
                $("#chkAutoGenerate").prop("disabled", false)
                if ($("#chkAutoGenerate").is(':checked'))
                    $("#txtCateCode").prop("disabled", true);
                else
                    $("#txtCateCode").prop("disabled", false);
                reloadGrid();

            }
            else $.alert.open("Error", "Category name Required");
        });
        $('#grid').jtable({
            // title: 'Product List',
            paging: true,
            pageSize: 10,
            sorting: true,
            defaultSorting: 'Name ASC',
            actions: {
                listAction: function (postData, jtParams) {
                    console.log("Loading from custom function...");
                    return $.Deferred(function ($dfd) {
                        $.ajax({
                            //url: '/Product/GetProducts?jtStartIndex=' + jtParams.jtStartIndex + '&jtPageSize=' + jtParams.jtPageSize + '&jtSorting=' + jtParams.jtSorting,
                            url: '/Common/GetAllCategory',
                            type: 'POST',
                            dataType: 'json',
                            data: postData,
                            success: function (data) {
                                $dfd.resolve(data);
                            },
                            error: function () {
                                $dfd.reject();
                            }
                        });
                    });
                },
            },
            fields: {
                CategorySubCategoryID: {
                    key: true,
                    list: false,
                    create: false,
                    edit: false
                },
                CateorSubCateCode: {
                    title: 'Category Code',
                    width: '10%'

                },
                CategorySubCategoryName: {
                    title: 'Category Name',
                    width: '30%'

                },
                NameInBangla: {
                    title: 'Name In Bangla',
                    width: '30%'
                },

                EditLink: {
                    title: "Edit",
                    display: function (data) {
                        return "<div class='text-center'><a href='#' onclick='GetByID(" + data.record.CategorySubCategoryID + ",1)'><i class='fa fa-pencil-square-o'></i></a></div>";
                    }
                },
                DeleteLink: {
                    title: "Delete",
                    display: function (data) {
                        return '<div class="text-center delete-link"><a href="#" onclick="GetByID(' + data.record.CategorySubCategoryID + ',2)"><i class="fa fa-trash-o"></i></a></div>';
                    }
                }
            }
        });
        reloadGrid();
    });
    function reloadGrid() {
        $('#grid').jtable('load', { filterColumn: $('#filterColumn').val(), filterValue: $('#filterValue').val() });
    }
</script>


<h2 class="page-title">Category Info</h2>
<br />
<input type="hidden" id="hdnCategoryID" />

<div class="container">
    <div class="row">
        <div class="col-sm-6">
            <div class="form-group">
                <label class="control-label col-sm-4 required">Category Code</label>
                <div class="col-sm-6">
                    <input type="text" class="form-control" id="txtCateCode" placeholder="Category Code" />
                </div>
            </div>
        </div>
        <input type="checkbox" id="chkAutoGenerate" /><label for="chkAutoGenerate"> Auto Generate</label>
    </div>
    <br />
    <div class="row">
        <div class="col-sm-6">
            <div class="form-group">
                <label class="control-label col-sm-4 required">Category Name</label>
                <div class="col-sm-6">
                    <input type="text" class="form-control" id="txtCateName" placeholder="Category name" />
                </div>
            </div>
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-sm-6">
            <div class="form-group">
                <label class="control-label col-sm-4 required">Category Name(in Bangla)</label>
                <div class="col-sm-6">
                    <input type="text" id="txtCategoryInBangle" placeholder="Category name In Bangle" class="form-control" />
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-6">
            <div class="form-group">
                <input type="button" id="btnSave" value="Save" class="btn btn-primary" />
            </div>
        </div>
    </div>
</div>

<br />
<div id="grid"></div>
@section Scripts {
    <script src="~/Assets/js/app/CommonMethod.js?v=@gHRM.Core.Utilities.CommonHelper.DEV_VERSION()"></script>
    @Scripts.Render("~/bundles/jqueryui")
    @Scripts.Render("~/bundles/jtable")
}


