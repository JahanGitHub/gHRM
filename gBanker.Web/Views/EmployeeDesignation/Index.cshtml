@model IEnumerable<gHRM.Web.ViewModels.EmployeeDesignationViewModel>
<div class="row">
    <ol class="breadcrumb breadcrumb-arrow">
        <li><a href="/EmployeeDesignation">Designation</a></li>
    </ol>
</div>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLteLayout.cshtml";
}

<script type="text/javascript">
    function deleteConfirm(status) {
        if (status == "true") {
            return confirm('Are you sure you want to delete this record');
        }
        else {
            alert("You cannot delete this record because it is already disabled.");
            return false;
        }
    }

    function DesignationDelete(designationId) {
        debugger;
        $.alert.open('confirm', 'Are you sure you want to delete this record?', function (button) {
            if (button == 'yes') {
                $.ajax({
                    type: 'GET',
                    contentType: "application/json; charset=utf-8",
                    url: '@Url.Action("DesignationDelete", "EmployeeDesignation", "http")',
                    data: { designationId: designationId },
                    dataType: 'json',
                    async: true,
                    success: function (result) {
                        if (result == 1) {
                            loadEmployeeDesignation();
                            $.alert.open("Success", 'Employee Designation Delete Successfully.');
                        } else {
                            $.alert.open("Error", 'Employee Already Assigned Cannot Delete Designation.');
                        }
                    },
                    error: function (request, status, error) {
                        alert(request.statusText + "/" + request.statusText + "/" + error);
                    }
                });
                return true;
            }
            else {
                hiddenField.value = 'false';
                return false;
            }
        });
    }
    function loadEmployeeDesignation() {
        
        var dataSource = new kendo.data.DataSource({
            type: "aspnetmvc-ajax",
            pageSize: 25,
            schema: {
                data: "data", // records are returned in the "data" field of the response
                total: "total" // total number of records is in the "total" field of the response
            },
            serverPaging: true,   // enable server paging
            serverSorting: true,
            serverFiltering: true,
            transport: {
                read: {
                    url: '@Url.Action("GetDesignation", "EmployeeDesignation", "http")',
                    dataType: 'json',
                    //data: { FilterBy: filterBy, FilterValue: filterValue }
                }
            }
        });

        $("#grid").kendoGrid({
            dataSource: dataSource,
            height: 600,
            groupable: false,
            reorderable: true,
            filterable: true,
            sortable: true,

            selectable: false,
            resizable: true,
            pageable: {
                refresh: true,
                pageSizes: true,
                buttonCount: 5
            },
            columns: [
                 {
                     field: "DesignationId",
                     hidden: true,
                     filterable: false
                 },
                 {
                     field: "rowSl",
                     title: "Sl No.",
                     width: "50px",
                     filterable: false,
                     columnMenu: false,
                     groupable: false,
                     reorderable: false,
                     sortable: false
                     //locked: true,
                     //lockable: false,
                 },
                 {
                     width: "150px",
                     field: "DesignationName",
                     filterable: true,
                     title: "Designation Name",
                     //locked: true
                 },
                 {
                     width: "80px",
                     field: "DesignationCode",
                     title: "Code",
                     //locked: true
                 },
                {
                    width: "50px",
                    field: "Rank",
                    title: "Rank",
                    //locked: true
                },
                {
                    width: "100px",
                    field: "DesignationShortName",
                    title: "Short Name"
                },

                {
                    width: "100px",
                    field: "InsuranceAmount",
                    title: "Insurance Amount"
                },

                 {
                     width: "50px",
                     title: 'Edit',
                     template: function (dataItem) {
                         return "<a href='/EmployeeDesignation/Edit/" + dataItem.DesignationId + "'><i class='fa fa-pencil-square-o'></i></a>";///Office/Create/?OfficeId=" + dataItem.EntrepreneurId + "
                         //return "<div class='text-center'><a href='/EmployeeDesignation/Edit/" + data.record.DesignationId + "'><i class='fa fa-pencil-square-o'></i></a></div>";
                     }
                 },
                 {
                     width: "50px",
                     title: 'Delete',
                     template: function (dataItem) {
                         return "<a href='#' OnClick='DesignationDelete(" + dataItem.DesignationId + ");'><i class='fa fa-trash-o'></i></a>";///Office/Create/?OfficeId=" + dataItem.EntrepreneurId + "
                         //return '<div class="text-center"><a href="#" OnClick="DesignationDelete(' + data.record.DesignationId + ');"><i class="fa fa-trash-o"></i></a></div>';
                     }
                 }
            ]
        });
    }

    $(document).ready(function () {
        loadEmployeeDesignation();
    });
   
</script>



@Html.ActionLink("Add New", "Create", null, new { @class = "btn btn-success btn-sm pull-right" })
&nbsp;
<h2 class="page-title">Payroll Designation List (পে-রোল পদবীর তালিকা)</h2>
&nbsp;
@*<div class="row">
    <div class="col-md-2 text-right">
        <label class="control-label" style="padding-top:8px;">Filter By:</label>
    </div>
    <div class="col-md-4">
        <select id="filterColumn" class="form-control">
            <option value="ViewAll">View All</option>
            <option value="DesignationName">Designation Name</option>
            <option value="DesignationCode">Designation Code</option>
        </select>
    </div>
    <div class="col-md-3">
        <input type="text" id="filterValue" placeholder="Type Search Text" class="form-control" />
    </div>
    <div class="col-md-3">
        <input type="button" id="btnSearch" value="Search" class="btn btn-primary" onclick="reloadGrid();" />
    </div>
</div>*@

<div class="row">
   
   
    <div id="grid">

    </div>

</div>

@section Scripts {

    @Scripts.Render("~/bundles/jqueryui")
    @Scripts.Render("~/bundles/jtable")
    @Scripts.Render("~/bundles/alert")
}
