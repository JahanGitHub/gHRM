@model IEnumerable<gHRM.Web.ViewModels.StateOrProvinceViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLteLayout.cshtml";
}

<script type="text/javascript">
    function deleteConfirm(status) {
        if (status == "true") {
            return confirm('Are you sure you want to delete this record');
        }
        else {
            alert("You cannot delete this record because it is already disabled.");
            return false;
        }
    }

    @*function GetCountry() {
        var ddlCountry = $("#ddlCountry");
        $.ajax({
            type: 'GET',
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetCountryList", "StateOrProvince", "http")',
            data: {},
            dataType: 'json',
            async: true,
            success: function (data) {
                ddlCountry.html('');
                $.each(data, function (id, option) {
                    ddlCountry.append($('<option></option>').val(option.Value).html(option.Text));
                });
            },
            error: function (request, status, error) {
                alert(request.statusText + "/" + request.statusText + "/" + error);
            }
        });
    }*@
    function loadEmployeeDesignation() {

        var dataSource = new kendo.data.DataSource({
            type: "aspnetmvc-ajax",
            pageSize: 25,
            schema: {
                data: "data", // records are returned in the "data" field of the response
                total: "total" // total number of records is in the "total" field of the response
            },
            serverPaging: true,   // enable server paging
            serverSorting: true,
            serverFiltering: true,
            transport: {
                read: {
                    url: '@Url.Action("GetStates", "StateOrProvince", "http")',
                    dataType: 'json',
                    //data: { FilterBy: filterBy, FilterValue: filterValue }
                }
            }
        });

        $("#grid").kendoGrid({
            dataSource: dataSource,
            //height: 400,
            groupable: false,
            reorderable: true,
            filterable: true,
            sortable: true,

            selectable: false,
            resizable: true,
            pageable: {
                refresh: true,
                pageSizes: true,
                buttonCount: 5
            },
            columns: [
                 {
                     field: "StateOrProvinceId",
                     hidden: true,
                     filterable: false
                 },
                 {
                     field: "CountryId",
                     hidden: true,
                     filterable: false
                 },
                 {
                     field: "rowSl",
                     title: "Sl No.",
                     width: "50px",
                     filterable: false,
                     columnMenu: false,
                     groupable: false,
                     reorderable: false,
                     sortable: false
                     //locked: true,
                     //lockable: false,
                 },
                 {
                     width: "150px",
                     field: "CountryName",
                     filterable: true,
                     title: "Country Name",
                     //locked: true
                 },
                 {
                     width: "150px",
                     field: "Name",
                     title: "Division Name",
                     //locked: true
                 },
                {
                    width: "100px",
                    field: "Code",
                    title: "Division Code",
                    //locked: true
                },

                 {
                     width: "50px",
                     title: 'Edit',
                     template: function (dataItem) {
                         return "<a href='/StateOrProvince/Edit/" + dataItem.StateOrProvinceId + "'><i class='fa fa-pencil-square-o'></i></a>";///Office/Create/?OfficeId=" + dataItem.EntrepreneurId + "

                     }
                 },
                 //{
                 //    width: "50px",
                 //    title: 'Delete',
                 //    template: function (dataItem) {
                 //        return "<a href='#' OnClick='DesignationDelete(" + dataItem.DesignationId + ");'><i class='fa fa-trash-o'></i></a>";///Office/Create/?OfficeId=" + dataItem.EntrepreneurId + "
                 //        //return '<div class="text-center"><a href="#" OnClick="DesignationDelete(' + data.record.DesignationId + ');"><i class="fa fa-trash-o"></i></a></div>';
                 //    }
                 //}
            ]
        });
    }

    $(document).ready(function () {
        loadEmployeeDesignation();

        @*$("#ddlCountry").change(function () {
            var country_val = $("#ddlCountry").val();
            if (country_val > 0) {
                var ddlState = $("#ddlState");
                $.ajax({
                    type: 'GET',
                    contentType: "application/json; charset=utf-8",
                    url: '@Url.Action("GetStateOrProvinceSearch", "StateOrProvince", "http")',
                    data: { country_val: country_val },
                    dataType: 'json',
                    async: true,
                    success: function (data) {
                        ddlState.html('');
                        $.each(data, function (id, option) {
                            ddlState.append($('<option></option>').val(option.Value).html(option.Text));
                        });
                    },
                    error: function (request, status, error) {
                        alert(request.statusText + "/" + request.statusText + "/" + error);
                    }
                });
            }
            else {
                $("#ddlState").empty();
            }
        });*@
    });


</script>

@Html.ActionLink("Add New", "Create", null, new { @class = "btn btn-success btn-sm pull-right" })
&nbsp;
<h2 class="page-title">State Or Province Or Division List (বিভাগের সুচী)</h2>
&nbsp;
@*<div class="row">
        <div class="col-md-5">
            <div class="form-group">
                @Html.Label("Country (দেশের নাম)", new { @class = "control-label col-md-4" })
                <div class="col-md-8">
                    @Html.DropDownList("CountryList", null, new { @ID = "ddlCountry", @class = "form-control" })
                </div>
            </div>
            </div>
        <div class="col-md-6">
            <div class="form-group">
                @Html.Label("StateOrProvince (বিভাগের নাম)", new { @class = "control-label col-md-4" })
                <div class="col-md-8">
                    @Html.DropDownList("StateList", null, new { @ID = "ddlState", @class = "form-control" })
                </div>
            </div>
        </div>
        <div class="col-md-1">
            <div class="form-group">
                <input type="button" id="btnSearch" value="Search" class="btn btn-primary" onclick="reloadGrid();" />
            </div>
        </div>
    </div>*@
<div class="row">
    &nbsp;
    <div id="grid"></div>
</div>

@section Scripts {

    @Scripts.Render("~/bundles/jqueryui")
    @Scripts.Render("~/bundles/jtable")
}
