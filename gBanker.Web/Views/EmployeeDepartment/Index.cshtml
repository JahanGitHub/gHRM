@model gHRM.Web.ViewModels.EmployeeDepartmentViewModel
<div class="row">
    <ol class="breadcrumb breadcrumb-arrow">
        <li><a href="/EmployeeDepartment">Department</a></li>
    </ol>
</div>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLteLayout.cshtml";
}

<script type="text/javascript">
    function deleteConfirm(status) {
        if (status == "true") {
            return confirm('Are you sure you want to delete this record');
        }
        else {
            alert("You cannot delete this record because it is already disabled.");
            return false;
        }
    }

    function departmentDelete(departmentId) {
        $.alert.open('confirm', 'Are you sure you want to delete this record?', function (button) {
            if (button == 'yes') {
                $.ajax({
                    type: 'GET',
                    contentType: "application/json; charset=utf-8",
                    url: '@Url.Action("departmentDelete", "EmployeeDepartment", "http")',
                    data: { departmentId: departmentId },
                    dataType: 'json',
                    async: true,
                    success: function (data) {
                        if (data.result==1) {
                            $.alert.open("Success", data.message);
                            $('#grid').jtable('load');
                        } else {
                            $.alert.open("Error", data.message);
                        }
                        
                    },
                    error: function (request, status, error) {
                        alert(request.statusText + "/" + request.statusText + "/" + error);
                    }
                });
                return true;
            }
            else {
                hiddenField.value = 'false';
                return false;
            }
        });
    }

    $(document).ready(function () {
        ///kendo grid

        @*$('#grid').html("");
        var dataSource = new kendo.data.DataSource({
            type: "aspnetmvc-ajax",
            pageSize: 25,
            schema: {
                data: "data", // records are returned in the "data" field of the response
                total: "total" // total number of records is in the "total" field of the response
            },
            serverPaging: true,   // enable server paging
            serverSorting: true,
            serverFiltering: true,
            transport: {
                read: {
                    //url: baseURL + '/ProcessCategory/GetProcessCategoryList?QueryOption=' + 1,
                    url: '@Url.Action("GetDepartment", "EmployeeDepartment", "http")',
                    dataType: 'json',
                    data: {}
                }
            }
        });
        debugger;
        $("#grid").kendoGrid({
            
            dataSource: dataSource,
            groupable: false,
            reorderable: true,
            filterable: true,
            sortable: true,

            selectable: false,
            resizable: true,
            pageable: {
                refresh: true,
                pageSizes: true,
                buttonCount: 5
            },
            columns: [
                //{
                //    field: "DepartmentId",
                //    hidden: true,
                //    filterable: false
                //},
                //{
                //    field: "rowSl",
                //    title: "SL",
                //    width: "20px",
                //    filterable: false,
                //},//OfficeTypeName
                //{
                //    field: "OfficeTypeName",
                //    title: "Office Type",
                //    width: "50px",
                //    filterable: true,
                //},
                {                    
                    field: "DepartmentCode",
                    title: "Department Code",
                    width: "50px",
                    filterable: true,
                },
            //    {
            //        field: "DepartmentName",
            //        title: "Department Name",
            //        width: "100px",
            //        filterable: true,
            //    },
            //    {
            //        field: "DepartmentShortName",
            //        title: "Short Name",
            //        width: "50px",
            //        filterable: false,
            //    },             
               
            //     {
            //         title: "Edit",
            //         width: "20px",
            //         template: function (dataItem) {
            //             if (dataItem.OfficeTypeId != 1) {
            //                 return "<a href='/EmployeeDepartment/Edit/" + dataItem.DepartmentId + "'><i class='fa fa-pencil-square-o'></i></a>";
            //             } else {
            //                 return "";
            //             }
            //         }
            //     },
            //{
            //    title: "Delete",
            //    width: "25px",
            //    template: function (dataItem) {
            //        if (dataItem.OfficeTypeId != 1) {
            //            return "<a href='#' OnClick='departmentDelete(" + dataItem.DepartmentId + ");'><i class='fa fa-trash-o'></i></a>";
            //        } else {
            //            return "";
            //        }
            //    }
            //}
            ]
        });*@



        /// kendo grid end




        $('#grid').jtable({

            paging: true,
            pageSize: 10,
            sorting: true,
            defaultSorting: 'Name ASC',
            actions: {

                listAction: '/EmployeeDepartment/GetDepartment'

            },
            fields: {
                DepartmentId: {
                    key: true,
                    list: false,
                    create: false,
                    edit: false
                },
                DepartmentCode: {
                    title: 'Code'
                },
                OfficeTypeName: {
                    title: '  Office Type'
                },
                DepartmentName: {
                    title: 'Department Name'
                },
                DepartmentShortName: {
                    title: 'Short Name'
                },

                EditLink: {
                    title: "Edit",
                    sorting: false,
                    display: function (data) {
                        return "<div class='text-center'><a href='/EmployeeDepartment/Edit/" + data.record.DepartmentId + "'><i class='fa fa-pencil-square-o'></i></a></div>";
                    }
                },
                Delete: {
                    title: "Delete",
                    display: function (data) {

                        return '<div class="text-center"><a href="#" OnClick="departmentDelete(' + data.record.DepartmentId + ');"><i class="fa fa-trash-o"></i></a></div>';
                    }

                },

            }
        });
        reloadGrid();
        $("#filterColumn").change(function () {
            if ($(this).val() === "ViewAll") {
                $("#filterValue").val('');
            }
        });

    });
    function reloadGrid() {
        
        $('#grid').jtable('load', { filterColumn: $('#filterColumn').val(), filterValue: $('#filterValue').val() });
       
    }

</script>
@Html.ActionLink("Add New", "Create", null, new { @class = "btn btn-success btn-sm pull-right" })
&nbsp;
<h2 class="page-title">Department List(বিভাগের তালিকা)</h2>
&nbsp;
<div class="row">
    <div class="col-md-2 text-right">
        <label class="control-label" style="padding-top:8px;">Filter By:</label>
    </div>
    <div class="col-md-4">
        <select id="filterColumn" class="form-control">
            <option value="ViewAll">View All</option>
            <option value="DepartmentCode">Department Code</option>
            <option value="DepartmentName">Department Name</option>
        </select>
    </div>
    <div class="col-md-3">
        <input type="text" id="filterValue" placeholder="Type Search Text" class="form-control" />
    </div>
    <div class="col-md-3">
        <input type="button" id="btnSearch" value="Search" class="btn btn-primary" onclick="reloadGrid();" />
    </div>
</div>
&nbsp;
<div class="row">
    <div class="col-md-12">
        &nbsp;
    </div>
      <div id="grid"></div>
    @*<div class="table-responsive">
        <div id="grid"></div>
    </div>*@
</div>

@section Scripts {

    @Scripts.Render("~/bundles/jqueryui")
    @Scripts.Render("~/bundles/jtable")
    @Scripts.Render("~/bundles/alert")
}


